{
  "name": "alyra-website",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "eleventy --serve",
    "build": "eleventy",
    "deploy": "npm run build && npx wrangler deploy"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.5",
    "wrangler": "^4.0.0"
  }
}
